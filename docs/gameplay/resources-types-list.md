# Resources Types

Every resource type sets what material crafts its sapling, the tint of its leaves and fragments, how often its leaves drop saplings and fragments, and how long it takes in the Tree Simulator.

Each resource type grows as every tree type, so the mod adds **46 × 9 = 414** saplings and leaves blocks, plus one leaf fragment per resource type.

## Built-in resources types

Use the tree buttons to preview every sapling and leaves block in a different shape.

<ResourceTypeTable />

## Built-in tree types

<TreeTypeTable />

## Adding your own

- [Custom resources types](../datapacks/resources-types) and [custom tree types](../datapacks/tree-types) with JSON files
- [Plugin API](../developer/plugin-api) to register them from Java
